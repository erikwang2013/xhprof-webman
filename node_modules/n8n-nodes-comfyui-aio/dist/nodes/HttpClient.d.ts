import { Logger } from './logger';
export interface HttpRequestConfig {
    method: 'GET' | 'POST' | 'PUT' | 'DELETE' | 'PATCH';
    url: string;
    headers?: Record<string, string>;
    body?: unknown;
    json?: boolean;
    qs?: Record<string, unknown>;
    encoding?: string;
    timeout?: number;
    abortSignal?: AbortSignal;
}
export interface IHttpAdapter {
    request<T>(config: HttpRequestConfig): Promise<T>;
}
export interface HttpClientConfig {
    adapter: IHttpAdapter;
    logger?: Logger;
    defaultTimeout?: number;
}
export declare class HttpClient {
    private adapter;
    private logger;
    private defaultTimeout;
    constructor(config: HttpClientConfig);
    request<T = unknown>(config: HttpRequestConfig): Promise<T>;
    get<T = unknown>(url: string, options?: Omit<HttpRequestConfig, 'method' | 'url'>): Promise<T>;
    post<T = unknown>(url: string, body?: unknown, options?: Omit<HttpRequestConfig, 'method' | 'url' | 'body'>): Promise<T>;
    put<T = unknown>(url: string, body?: unknown, options?: Omit<HttpRequestConfig, 'method' | 'url' | 'body'>): Promise<T>;
    delete<T = unknown>(url: string, options?: Omit<HttpRequestConfig, 'method' | 'url'>): Promise<T>;
    patch<T = unknown>(url: string, body?: unknown, options?: Omit<HttpRequestConfig, 'method' | 'url' | 'body'>): Promise<T>;
    private normalizeError;
}
//# sourceMappingURL=HttpClient.d.ts.map