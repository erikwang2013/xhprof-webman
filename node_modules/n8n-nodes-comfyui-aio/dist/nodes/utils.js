"use strict";
/**
 * Utility functions for ComfyUI nodes
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.extractFileInfo = extractFileInfo;
exports.extractImageFileInfo = extractImageFileInfo;
exports.extractVideoFileInfo = extractVideoFileInfo;
exports.extractAudioFileInfo = extractAudioFileInfo;
exports.validateFilename = validateFilename;
exports.generateUniqueFilename = generateUniqueFilename;
exports.validateBufferSize = validateBufferSize;
exports.validateMimeType = validateMimeType;
exports.getMaxImageSizeBytes = getMaxImageSizeBytes;
exports.getMaxVideoSizeBytes = getMaxVideoSizeBytes;
exports.getMaxAudioSizeBytes = getMaxAudioSizeBytes;
exports.getMaxBase64Length = getMaxBase64Length;
exports.getMaxVideoBase64Length = getMaxVideoBase64Length;
exports.getMaxAudioBase64Length = getMaxAudioBase64Length;
exports.formatBytes = formatBytes;
const crypto_1 = require("crypto");
const constants_1 = require("./constants");
/**
 * Extract file information from a path
 * @param path - File path (can include query parameters)
 * @param defaultExt - Default extension if none found
 * @param mimeType - MIME type for fallback
 * @returns File information
 */
function extractFileInfo(path, defaultExt, mimeType) {
    let filename;
    // Try to extract filename from URL query parameters first (ComfyUI format)
    // ComfyUI URLs look like: /view?filename=image_12345.png&subfolder=&type=output
    if (path.includes('filename=')) {
        const filenameMatch = path.match(/filename=([^&]+)/);
        if (filenameMatch) {
            filename = decodeURIComponent(filenameMatch[1]);
        }
        else {
            // Fallback: extract from path
            filename = path.split('/').pop() || `file_${(0, crypto_1.randomUUID)()}.${defaultExt}`;
        }
    }
    else {
        // No query parameters, extract from path
        filename = path.split('/').pop() || `file_${(0, crypto_1.randomUUID)()}.${defaultExt}`;
    }
    // Remove query parameters if still present
    if (filename.includes('?')) {
        filename = filename.split('?')[0];
    }
    // Extract extension
    let ext = defaultExt;
    const extMatch = filename.match(/\.([^.]+)$/);
    if (extMatch) {
        ext = extMatch[1].toLowerCase();
    }
    // If no extension in filename and type is specified in path
    if (!extMatch && path.includes('type=')) {
        const typeMatch = path.match(/type=([^&]+)/);
        if (typeMatch) {
            const type = typeMatch[1].toLowerCase();
            const mimeMap = {
                'input': 'png',
                'output': 'png',
                'temp': 'png',
            };
            ext = mimeMap[type] || defaultExt;
        }
    }
    // Determine MIME type
    let finalMimeType = mimeType;
    if (!finalMimeType) {
        // Try to get MIME type from extension
        const imageMimeType = constants_1.IMAGE_MIME_TYPES[ext];
        const videoMimeType = constants_1.VIDEO_MIME_TYPES[ext];
        finalMimeType = imageMimeType || videoMimeType || 'application/octet-stream';
    }
    // If filename doesn't have extension, add it
    if (!extMatch && ext !== defaultExt) {
        filename = `${filename}.${ext}`;
    }
    return {
        filename,
        extension: ext,
        mimeType: finalMimeType,
    };
}
/**
 * Extract file information for images
 * @param path - File path
 * @param defaultExt - Default extension
 * @returns File information with image MIME type
 */
function extractImageFileInfo(path, defaultExt = 'png') {
    const fileInfo = extractFileInfo(path, defaultExt);
    // Ensure the MIME type is an image type
    if (!Object.values(constants_1.IMAGE_MIME_TYPES).includes(fileInfo.mimeType)) {
        // If the detected MIME type is not an image, use the default extension
        const defaultMime = constants_1.IMAGE_MIME_TYPES[defaultExt];
        if (defaultMime) {
            fileInfo.mimeType = defaultMime;
        }
    }
    return fileInfo;
}
/**
 * Extract file information for videos
 * @param path - File path
 * @param defaultExt - Default extension
 * @returns File information with video MIME type
 */
function extractVideoFileInfo(path, defaultExt = 'mp4') {
    const fileInfo = extractFileInfo(path, defaultExt);
    // Ensure the MIME type is a video type
    if (!Object.values(constants_1.VIDEO_MIME_TYPES).includes(fileInfo.mimeType)) {
        // If the detected MIME type is not a video, use the default extension
        const defaultMime = constants_1.VIDEO_MIME_TYPES[defaultExt];
        if (defaultMime) {
            fileInfo.mimeType = defaultMime;
        }
    }
    return fileInfo;
}
/**
 * Extract file information for audio
 * @param path - File path
 * @param defaultExt - Default extension
 * @returns File information with audio MIME type
 */
function extractAudioFileInfo(path, defaultExt = 'mp3') {
    const fileInfo = extractFileInfo(path, defaultExt);
    // Ensure the MIME type is an audio type
    if (!Object.values(constants_1.AUDIO_MIME_TYPES).includes(fileInfo.mimeType)) {
        // If the detected MIME type is not an audio, use the default extension
        const defaultMime = constants_1.AUDIO_MIME_TYPES[defaultExt];
        if (defaultMime) {
            fileInfo.mimeType = defaultMime;
        }
    }
    return fileInfo;
}
/**
 * Validate and sanitize filename
 * @param filename - The filename to validate
 * @returns Sanitized filename
 * @throws Error if filename is invalid
 */
function validateFilename(filename) {
    if (!filename || typeof filename !== 'string') {
        throw new Error('Filename must be a non-empty string');
    }
    // Check for path traversal attempts
    if (filename.includes('..') || filename.includes('/') || filename.includes('\\')) {
        throw new Error('Filename cannot contain path traversal characters (.., /, \\)');
    }
    // Limit filename length
    const MAX_FILENAME_LENGTH = 255;
    if (filename.length > MAX_FILENAME_LENGTH) {
        throw new Error(`Filename too long (maximum ${MAX_FILENAME_LENGTH} characters)`);
    }
    // Remove null bytes
    filename = filename.replace(/\0/g, '');
    if (filename.length === 0) {
        throw new Error('Filename cannot be empty after sanitization');
    }
    return filename;
}
/**
 * Generate a unique filename using UUID
 * @param extension - File extension (without dot)
 * @param prefix - Optional prefix for the filename
 * @returns Unique filename
 */
function generateUniqueFilename(extension, prefix = 'file') {
    const uuid = (0, crypto_1.randomUUID)();
    const ext = extension.startsWith('.') ? extension.slice(1) : extension;
    return `${prefix}_${uuid}.${ext}`;
}
/**
 * Validate buffer size before processing
 * @param buffer - The buffer to validate
 * @param maxSizeMB - Maximum size in megabytes
 * @param context - Context for error message
 * @throws Error if buffer exceeds maximum size
 */
function validateBufferSize(buffer, maxSizeMB = constants_1.VALIDATION.MAX_IMAGE_SIZE_MB, context = 'Buffer') {
    if (!Buffer.isBuffer(buffer)) {
        throw new Error(`${context}: Invalid buffer type`);
    }
    if (buffer.length === 0) {
        throw new Error(`${context}: Buffer is empty`);
    }
    const maxSizeBytes = maxSizeMB * 1024 * 1024;
    if (buffer.length > maxSizeBytes) {
        const sizeMB = Math.round(buffer.length / 1024 / 1024);
        throw new Error(`${context}: Size (${sizeMB}MB) exceeds maximum allowed size of ${maxSizeMB}MB`);
    }
}
/**
 * Validate MIME type against allowed types
 * @param mimeType - MIME type to validate
 * @param allowedTypes - Record of allowed MIME types
 * @returns Validated MIME type
 * @throws Error if MIME type is invalid
 */
function validateMimeType(mimeType, allowedTypes) {
    if (!mimeType || typeof mimeType !== 'string') {
        throw new Error('MIME type must be a non-empty string');
    }
    const normalized = mimeType.toLowerCase();
    const allowedValues = Object.values(allowedTypes);
    if (!allowedValues.includes(normalized)) {
        throw new Error(`Unsupported MIME type "${normalized}". Allowed types: ${allowedValues.join(', ')}`);
    }
    return normalized;
}
/**
 * Get maximum image size in bytes
 * @returns Maximum size in bytes (from VALIDATION.MAX_IMAGE_SIZE_MB)
 */
function getMaxImageSizeBytes() {
    return constants_1.VALIDATION.MAX_IMAGE_SIZE_MB * 1024 * 1024;
}
/**
 * Get maximum video size in bytes
 * @returns Maximum size in bytes (from VALIDATION.MAX_VIDEO_SIZE_MB)
 */
function getMaxVideoSizeBytes() {
    return constants_1.VALIDATION.MAX_VIDEO_SIZE_MB * 1024 * 1024;
}
/**
 * Get maximum audio size in bytes
 * @returns Maximum size in bytes (from VALIDATION.MAX_AUDIO_SIZE_MB)
 */
function getMaxAudioSizeBytes() {
    return constants_1.VALIDATION.MAX_AUDIO_SIZE_MB * 1024 * 1024;
}
/**
 * Get maximum Base64 encoded length for an image
 * Base64 encoding increases size by approximately 33% (4/3 ratio)
 * @returns Maximum Base64 length in bytes
 */
function getMaxBase64Length() {
    return getMaxImageSizeBytes() * 4 / 3;
}
/**
 * Get maximum Base64 encoded length for a video
 * Base64 encoding increases size by approximately 33% (4/3 ratio)
 * @returns Maximum Base64 length in bytes
 */
function getMaxVideoBase64Length() {
    return getMaxVideoSizeBytes() * 4 / 3;
}
/**
 * Get maximum Base64 encoded length for an audio
 * Base64 encoding increases size by approximately 33% (4/3 ratio)
 * @returns Maximum Base64 length in bytes
 */
function getMaxAudioBase64Length() {
    return getMaxAudioSizeBytes() * 4 / 3;
}
/**
 * Format bytes to human-readable size
 * @param bytes - Size in bytes
 * @param decimals - Number of decimal places (default: 2)
 * @returns Formatted size string (e.g., "1.5 MB")
 */
function formatBytes(bytes, decimals = 2) {
    if (bytes === 0)
        return '0 Bytes';
    const k = 1024;
    const dm = decimals < 0 ? 0 : decimals;
    const sizes = ['Bytes', 'KB', 'MB', 'GB', 'TB'];
    const i = Math.floor(Math.log(bytes) / Math.log(k));
    return parseFloat((bytes / Math.pow(k, i)).toFixed(dm)) + ' ' + sizes[i];
}
//# sourceMappingURL=utils.js.map